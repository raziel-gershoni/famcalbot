# Local Development Environment Variables
# Copy this file to .env.local and fill in your development credentials

# =============================================================================
# DEVELOPMENT BOT SETUP
# =============================================================================
# For local development, create a SEPARATE test bot:
# 1. Talk to @BotFather on Telegram
# 2. Create new bot: /newbot
# 3. Name it something like "YourBot Dev" or "YourBot Test"
# 4. Copy the token below
# 5. This bot will use POLLING mode (no webhook needed for local dev)
# =============================================================================

# Telegram Bot Configuration (DEV BOT - different from production)
TELEGRAM_BOT_TOKEN=your_dev_telegram_bot_token_here
TELEGRAM_BOT_USERNAME=your_dev_bot_username

# WhatsApp Business API Configuration (optional - same as prod or separate)
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your_random_secret_token_here

# Google Calendar API Configuration (same as production)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/api/refresh-token

# AI Provider Configuration (same as production or use cheaper models for testing)
AI_MODEL=claude-sonnet-4.5

# API Keys (same as production)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration
# Option 1: Use Neon branch (recommended)
# Create a dev branch: neon branches create --name dev
DATABASE_URL=your_dev_database_url_here
DIRECT_URL=your_dev_direct_url_here

# Option 2: Use local PostgreSQL
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/famcalbot_dev
# DIRECT_URL=postgresql://postgres:postgres@localhost:5432/famcalbot_dev

# Cron Job Secret (can be different from production)
CRON_SECRET=dev_secret_token_here

# Redis (Upstash) - same as production or separate dev instance
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here

# Optional: Voice Messages (Google Cloud TTS)
# GOOGLE_TTS_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"..."}
# VOICE_DEFAULT=he-IL-Wavenet-B
# VOICE_SPEED=1.0

# Environment (REQUIRED for local dev)
NODE_ENV=development

# =============================================================================
# DEVELOPMENT WORKFLOW
# =============================================================================
# 1. Start Next.js dev server:
#    npm run dev
#
# 2. Bot automatically runs in POLLING mode (no webhook setup needed)
#
# 3. Test by chatting with your dev bot on Telegram
#
# 4. Changes are reflected immediately with Hot Module Reload (HMR)
#
# 5. Use Vercel preview deployments for staging tests:
#    git push origin feature-branch
#    → Vercel creates preview URL
# =============================================================================
